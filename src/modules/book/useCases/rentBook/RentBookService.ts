import { BookRepository } from "../../infra/repositories/BookRepository"

class RentBookService {
    async execute() {
        const bookRepository = new BookRepository();
    
        const rentedBook = bookRepository.
    
        return rentedBook;
    }
}

export { RentBookService }